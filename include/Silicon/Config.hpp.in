#ifndef SI_CONFIG_HPP
#define SI_CONFIG_HPP

namespace Si {

enum class BuildConfig {
    Debug,
    Release
};

} // namespace Si

constexpr Si::BuildConfig SI_BUILD_CONFIG = Si::BuildConfig::${CMAKE_BUILD_TYPE};

#endif // SI_CONFIG_HPP